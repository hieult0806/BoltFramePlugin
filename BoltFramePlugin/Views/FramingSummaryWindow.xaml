<Window x:Class="BoltFramePlugin.Views.FramingSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoltFramePlugin.Views"
        mc:Ignorable="d"
        Title="Framing Generation Summary" Height="450" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Framing Generation Summary" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="{Binding SummaryText}" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  AlternatingRowBackground="#F0F0F0"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Beam Type" Binding="{Binding BeamType}" Width="100"/>
                <DataGridTextColumn Header="Family" Binding="{Binding FamilyName}" Width="120"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="120"/>
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="60"/>
                <DataGridTextColumn Header="Min Length" Binding="{Binding MinLengthFt}" Width="90"/>
                <DataGridTextColumn Header="Max Length" Binding="{Binding MaxLengthFt}" Width="90"/>
                <DataGridTextColumn Header="Avg Length" Binding="{Binding AvgLengthFt}" Width="90"/>
                <DataGridTextColumn Header="Total Length" Binding="{Binding TotalLengthFt}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="OK" Width="80" Height="30" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>
