<UserControl x:Class="BoltFramePlugin.Views.UserControls.LimitingDistance.ViewTrackerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Created Elevation Views" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Track and manage elevation views created for wall projections"
                       FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Views DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding CreatedViews}"
                  SelectedItem="{Binding SelectedView, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  IsReadOnly="True"
                  Margin="0,0,0,10"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="View Name" Binding="{Binding ViewName}" Width="*" MinWidth="150"/>
                <DataGridTextColumn Header="View Type" Binding="{Binding ViewType}" Width="100"/>
                <DataGridTextColumn Header="Element ID" Binding="{Binding ElementId}" Width="100"/>
                <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat='yyyy-MM-dd HH:mm'}" Width="130"/>
                <DataGridTextColumn Header="Walls Count" Binding="{Binding WallsCount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Open View"
                    Command="{Binding OpenViewCommand}"
                    Width="100" Height="30" Margin="0,0,5,0"
                    IsEnabled="{Binding IsViewSelected}"/>
            <Button Content="Delete View"
                    Command="{Binding DeleteViewCommand}"
                    Width="100" Height="30" Margin="0,0,5,0"
                    IsEnabled="{Binding IsViewSelected}"
                    Background="#FFE74C3C" Foreground="White"/>
            <Button Content="Delete All"
                    Command="{Binding DeleteAllViewsCommand}"
                    Width="100" Height="30"
                    Background="#FFC0392B" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>
