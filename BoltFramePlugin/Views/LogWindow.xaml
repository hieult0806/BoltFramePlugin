<Window x:Class="BoltFramePlugin.Views.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Debug Logs" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Clear" Command="{Binding ClearLogsCommand}"
                        Width="80" Height="30" Margin="0,0,10,0"/>
                <CheckBox Content="Auto-scroll" IsChecked="{Binding AutoScroll}"
                          VerticalAlignment="Center" Margin="0,0,20,0"/>
                <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox SelectedItem="{Binding FilterLevel}" Width="120" Height="25" Margin="0,0,20,0">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="Information"/>
                    <ComboBoxItem Content="Warning"/>
                    <ComboBoxItem Content="Error"/>
                </ComboBox>
                <TextBlock Text="Regex Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding RegexFilter, UpdateSourceTrigger=PropertyChanged}"
                         Width="250" Height="25" VerticalContentAlignment="Center"
                         ToolTip="Enter regex pattern to filter log messages (leave empty to show all)"/>
                <CheckBox Content="Case Sensitive" IsChecked="{Binding CaseSensitive}"
                          VerticalAlignment="Center" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Log Display -->
        <TextBox Grid.Row="1"
                 Text="{Binding LogText, Mode=OneWay}"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="11"
                 Padding="5"
                 Background="#1E1E1E"
                 Foreground="#D4D4D4"
                 x:Name="LogTextBox"
                 TextChanged="LogTextBox_TextChanged"/>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#F0F0F0" Padding="5">
            <TextBlock Text="{Binding StatusText}" FontSize="10"/>
        </Border>
    </Grid>
</Window>
