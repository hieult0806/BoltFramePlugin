<Window x:Class="BoltFramePlugin.Views.ConfigurationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoltFramePlugin.Views"
        mc:Ignorable="d"
        Title="ConfigurationWindow" Height="450" Width="548">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="89*"/>
            <ColumnDefinition Width="185*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="8">
            <Button Content="WorkingSpace"></Button>
            <Button Content="About"></Button>
        </StackPanel>
        <DockPanel Grid.Column="1" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="9*"></RowDefinition>
                    <RowDefinition Height="1*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBox TextWrapping="Wrap" Text="{Binding WorkingSpacePath}" Width="Auto" HorizontalAlignment="Stretch"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" FlowDirection="RightToLeft">
                    <Button Width="100" Height="32" Content="Save" Command="{Binding SaveCommand}"></Button>
                    <Button Width="100" Height="32" Content="Cancel" Command="{Binding CancelCommand}"></Button>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
