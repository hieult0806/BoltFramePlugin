<Window x:Class="LoBIM.Features.Framing.Views.BoltWallFrameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:LoBIM.Resources.Strings"
        xmlns:uc="clr-namespace:LoBIM.Views.Framing"
        xmlns:tree="clr-namespace:LoBIM.Views.UserControls"
        mc:Ignorable="d"
        Title="Wall Framing Configuration" Height="700" Width="800">
    <Grid Background="#FFECECEC">
        <Grid.RowDefinitions>
            <RowDefinition Height="32"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="42"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0" VerticalAlignment="Stretch">
            <ToolBar Height="32">
                <Button Command="New" Content="New" />
                <Separator/>
                <Button Command="Open" Content="Open" />
                <Button Command="{Binding OpenConfigurationWindowCommand}" Content="Configuration" />
            </ToolBar>
        </ToolBarTray>
        <Grid Grid.Row="1" Width="Auto" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0"  Background="#FFDBDBDB">
                <tree:TreeViewUC DataContext="{Binding TreeViewUcVM}"/>
            </DockPanel>
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <uc:InputWallFrameConfigurationUC DataContext="{Binding TopPlateConfiguration}"/>
                    <uc:InputWallFrameConfigurationUC DataContext="{Binding BottomPlateConfiguration}"/>
                    <uc:InputWallFrameConfigurationUC DataContext="{Binding ColumnConfiguration}"/>
                    <uc:InputWallFrameConfigurationUC DataContext="{Binding StudConfiguration}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <DockPanel Grid.Row="2" Background="#FFE5E5E5">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,0" VerticalAlignment="Center">
                <CheckBox IsChecked="{Binding CreateGroup}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="Create Group:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Text="{Binding GroupName}" Width="120" Height="24" IsEnabled="{Binding CreateGroup}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Width="90" Height="28" FontWeight="Bold" Content="Generate" Margin="8 0 8 0" Command="{Binding GenerateFrameCommand}"/>
                <Button Width="90" Height="28" FontWeight="Bold" Content="Cancel" Margin="8 0 8 0" Command="{Binding CloseCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
