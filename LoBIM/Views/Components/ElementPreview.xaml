<UserControl x:Class="LoBIM.Views.Components.ElementPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ev="clr-namespace:LoBIM.Helpers" 
             xmlns:local="clr-namespace:LoBIM.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" ev:MouseClickBehavior.Command="{Binding SelectCommand}" Background="Transparent">
        <Image Source="{Binding PreviewImage}" Width="48" Height="48"/>
        <StackPanel Margin="8 0 0 0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
            <TextBlock Text="{Binding FamilyName}"></TextBlock>
            <Separator/>
            <TextBlock Text="{Binding ElementName}"></TextBlock>
        </StackPanel>
    </StackPanel>
</UserControl>
