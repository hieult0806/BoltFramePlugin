<Window x:Class="LoBIM.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About LoBIM"
        Height="700"
        Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5"
        WindowStyle="None"
        AllowsTransparency="True"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>

        <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <Style x:Key="ContentTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="LineHeight" Value="18"/>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980B9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White"
            CornerRadius="8"
            BorderBrush="#BDC3C7"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000"
                            Opacity="0.2"
                            BlurRadius="20"
                            ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Close Button -->
            <Grid Grid.Row="0">
                <Border CornerRadius="8,8,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#3498DB" Offset="0"/>
                            <GradientStop Color="#2980B9" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <Grid Margin="30,25,30,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="LoBIM"
                                     FontSize="32"
                                     FontWeight="Bold"
                                     Foreground="White"/>
                            <TextBlock Text="Building Information Modeling"
                                     FontSize="14"
                                     Foreground="#ECF0F1"
                                     Margin="0,5,0,0"/>
                            <TextBlock Text="Revit Plugin for Structural Engineering"
                                     FontSize="12"
                                     Foreground="#BDC3C7"
                                     Margin="0,3,0,0"/>
                        </StackPanel>

                        <!-- Close Button -->
                        <Button Grid.Column="1"
                              Content="✕"
                              Width="30"
                              Height="30"
                              Background="Transparent"
                              Foreground="White"
                              BorderThickness="0"
                              FontSize="18"
                              FontWeight="Bold"
                              Cursor="Hand"
                              VerticalAlignment="Top"
                              Click="CloseButton_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                      CornerRadius="15">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                </Border>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1"
                       Margin="25,15,25,15">
                    <!-- Version and Description -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Version"/>
                            <TextBlock Style="{StaticResource ContentTextStyle}">
                                <Run Text="Version: " FontWeight="SemiBold"/>
                                <Run Text="1.0.0"/>
                            </TextBlock>
                            <TextBlock Style="{StaticResource ContentTextStyle}" Margin="0,3,0,0">
                                <Run Text="Build: " FontWeight="SemiBold"/>
                                <Run Text="{Binding BuildDate, Mode=OneWay}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="About"/>
                            <TextBlock Style="{StaticResource ContentTextStyle}"
                                     Text="A Revit plugin for structural engineering workflows in low-rise buildings."
                                     TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>

                    <!-- Credits Section -->
                    <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Development Team" Margin="0,8,0,5"/>

                    <!-- Credits Grid -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Developer -->
                        <Border Grid.Column="0" Grid.Row="0"
                              Background="#ECF0F1"
                              CornerRadius="6"
                              Padding="12">
                            <StackPanel>
                                <TextBlock Text="Lead Developer"
                                         FontSize="11"
                                         Foreground="#7F8C8D"
                                         FontWeight="SemiBold"/>
                                <TextBlock Text="LO TRUNG HIEU"
                                         FontSize="13"
                                         Foreground="#2C3E50"
                                         FontWeight="SemiBold"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Teammate -->
                        <Border Grid.Column="2" Grid.Row="0"
                              Background="#ECF0F1"
                              CornerRadius="6"
                              Padding="12">
                            <StackPanel>
                                <TextBlock Text="Team Member"
                                         FontSize="11"
                                         Foreground="#7F8C8D"
                                         FontWeight="SemiBold"/>
                                <TextBlock Text="MANOJ NANDANA"
                                         FontSize="13"
                                         Foreground="#2C3E50"
                                         FontWeight="SemiBold"
                                         Margin="0,4,0,0"/>
                                <TextBlock Text="Design &amp; Testing"
                                         FontSize="10"
                                         Foreground="#95A5A6"
                                         Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Instructors (spans both columns) -->
                        <Border Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3"
                              Background="#E8F6F3"
                              CornerRadius="6"
                              Padding="12"
                              BorderBrush="#16A085"
                              BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Academic Advisors &amp; Instructors"
                                         FontSize="11"
                                         Foreground="#16A085"
                                         FontWeight="SemiBold"
                                         Margin="0,0,0,6"/>

                                <!-- Instructors List -->
                                <TextBlock FontSize="10"
                                         Foreground="#2C3E50"
                                         TextWrapping="Wrap"
                                         LineHeight="16">
                                    <Run Text="Instructor 1" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 2" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 3" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 4" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 5" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 6" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 7" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 8" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 9" FontWeight="SemiBold"/><Run Text=", "/>
                                    <Run Text="Instructor 10" FontWeight="SemiBold"/>
                                </TextBlock>

                                <TextBlock Text="Guidance, Mentorship &amp; Technical Direction"
                                         FontSize="10"
                                         Foreground="#7F8C8D"
                                         FontStyle="Italic"
                                         Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Copyright -->
                    <TextBlock Style="{StaticResource ContentTextStyle}"
                             FontSize="10"
                             Foreground="#95A5A6"
                             TextAlignment="Center"
                             Margin="0,8,0,0">
                        <Run Text="Copyright © 2025 LoBIM Development Team. All rights reserved."/>
                    </TextBlock>

                    <!-- Acknowledgment Section -->
                    <Border Background="#FAFAFA"
                          CornerRadius="4"
                          Padding="10"
                          Margin="0,8,0,0"
                          BorderBrush="#E0E0E0"
                          BorderThickness="1">
                        <StackPanel Orientation="Horizontal"
                                  HorizontalAlignment="Center">
                            <TextBlock Text="Developed while studying at"
                                     FontSize="9"
                                     Foreground="#95A5A6"
                                     VerticalAlignment="Center"
                                     Margin="0,0,8,0"/>
                            <Image x:Name="SaitLogoImage"
                                 Width="30"
                                 Height="30"
                                 VerticalAlignment="Center"
                                 Stretch="Uniform"
                                 Margin="0,0,6,0"/>
                            <TextBlock Text="SAIT"
                                     FontSize="10"
                                     Foreground="#7F8C8D"
                                     FontWeight="SemiBold"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            <!-- Footer with OK Button -->
            <Border Grid.Row="2"
                  Background="#F8F9FA"
                  BorderBrush="#E0E0E0"
                  BorderThickness="0,1,0,0"
                  CornerRadius="0,0,8,8"
                  Padding="30,15">
                <Button Content="Close"
                      Style="{StaticResource CloseButtonStyle}"
                      HorizontalAlignment="Right"
                      Click="CloseButton_Click"/>
            </Border>
        </Grid>
    </Border>
</Window>
